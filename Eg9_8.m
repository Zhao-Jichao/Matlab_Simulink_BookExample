% Page262：已知状态空间模型，判断系统是否可控？是否可观？
clear;
clc;

A = [1 0 -1; -1 -2 0; 3 0 1];
B = [1 0; 2 1; 0 2];
C = [1 0 0; 0 -1 0];
% 计算可控判别矩阵
M = ctrb(A, B);
% 计算可控判别矩阵的秩
RM = rank(M);
% 计算可观判别矩阵
N = obsv(A, C);
% 计算可观判别矩阵的秩
RN = rank(N);
