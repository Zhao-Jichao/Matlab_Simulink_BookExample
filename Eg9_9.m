% Page263：已知状态空间模型，判断可控性，如果完全可控，转化为可控 II 型
clear;
clc;

A = [1 2 0; 3 -1 1; 0 2 0];
B = [2; 1; 1];
C = [0 0 1];
D = 0;
% 计算可控判别矩阵
T = ctrb(A, B);
% 计算可控判别矩阵的秩
R = rank(T);

% 计性状态空间的线性变换
[Ac2, Bc2, Cc2, Dc2] = ss2ss(A, B, C, D, inv(T));